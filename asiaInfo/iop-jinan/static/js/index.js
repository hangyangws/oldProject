;
! function(g, $, undefined) {
  'use strict';
  var $mainContent = $('#mainContent'),
    Project = {
      init: function() {},
    };

  /**
   * 项目初始化
   */
  Project.init();

  /**
   * 事件绑定
   */
  // $mainContent
  //     .on(_method, '.j-toggle-inner', Project.toggleInner);
}(window || this, jQuery);
